steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - Cloud/BBDD-Job/Dockerfile
      - -t
      - ${_IMAGE_URI}
      - .
images:
  - ${_IMAGE_URI}
substitutions:
  _IMAGE_URI: us-central1-docker.pkg.dev/PROJECT_ID/pii/bbdd-pii-job:latest
